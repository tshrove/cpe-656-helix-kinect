namespace Iava.Input.Camera {

    /// <summary>
    /// Specifies the joint tracking state.
    /// </summary>
    public enum IavaJointTrackingState {
        NotTracked = 0,
        Inferred = 1,
        Tracked = 2,
    }
}